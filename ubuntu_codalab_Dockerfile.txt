# Python requirements
FROM ubuntu:jammy

# Install anaconda
RUN apt-get update --fix-missing && apt-get install -y wget bzip2 ca-certificates \
    libglib2.0-0 libxext6 libsm6 libxrender1 \
    git mercurial subversion

# RUN apt-get update && apt-get install -y --no-install-recommends apt-utils

# http://bugs.python.org/issue19846
# > At the moment, setting "LANG=C" on a Linux system *fundamentally breaks Python 3*, and that's not OK.
ENV LANG C.UTF-8

# https://medium.com/@alexander.tyutin/docker-tips-ubuntu-and-stuck-of-interactive-tools-with-docker-build-1e95383cba96 :
ENV DEBIAN_FRONTEND=nonintercative

RUN apt-get install -y build-essential swig git

# C#, R, etc. requirements
RUN apt-get update && apt-get install -y --fix-missing r-base mono-runtime libgomp1 libc6

# Fix for 'xgboost' missing
# RUN conda install libgcc
# RUN conda install -c conda-forge tensorflow

RUN apt-get update && \
    apt-get -y install python3 python3-pip

RUN pip3 install \
        Cython \
        numpy \
        scipy \
        scikit-learn \
        pandas \
        pyyaml \
        imutils


# RUN git clone the 3 DAC4AutoML repos

COPY DAC4RL /root/DAC4RL
COPY DAC4SGD /root/DAC4SGD
COPY dac4automlcomp /root/dac4automlcomp
#COPY SGD_data /root/SGD_data

RUN cd /root/DAC4RL && pip3 install -e .
RUN cd /root/DAC4SGD && pip3 install -e .
RUN cd /root/dac4automlcomp && pip3 install -e .

#RUN which pip3

#RUN which python3
